# Read server-side by this remote's own server.js, at request time.
# This remote's own runtime environment — NOT used by the shell, and NOT
# related to Module Federation's remote resolution (that's remoteEntry.js,
# served as a static build artifact regardless of these values).
VITE_BASE_URL=http://localhost:6002
VITE_ONBOARDING_BASE_URL=http://localhost:7002
